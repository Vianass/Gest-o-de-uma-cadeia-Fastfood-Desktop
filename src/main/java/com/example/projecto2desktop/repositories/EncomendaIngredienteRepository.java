package com.example.projecto2desktop.repositories;

import com.example.projecto2desktop.models.EncomendaIngrediente;
import org.springframework.data.jpa.repository.JpaRepository;

public interface EncomendaIngredienteRepository extends JpaRepository<EncomendaIngrediente, Integer> {
}
